services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: telegraph
      POSTGRES_PASSWORD: telegraph
      POSTGRES_DB: telegraph
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: telegraph
      MINIO_ROOT_PASSWORD: telegraph123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
volumes:
  postgres_data:
  minio_data:
